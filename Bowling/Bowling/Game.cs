namespace Bowling
{
    public class Game
    {
        public int ShowResult(string frame)
        {
            //var parse = new Parse();
            //parse.Parser(frame);
            return 0;
        }
    }
}