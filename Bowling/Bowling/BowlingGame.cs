namespace Bowling
{
    public class BowlingGame
    {
        public int GameScore(string frame)
        {
            //var parse = new Parse();
            //var game = parse.Parser(frame);
            return 0;
        }
    }
}